# Static HTML prototype (archived)

Original mobile-first clickable prototype (`index.html` + `js/app.js`) wired to InnocenZ_MVP_v3_Final.xlsx.

The main app is now the React project in the repo root. This folder is kept for reference.
